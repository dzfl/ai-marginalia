# AI Marginalia — 仕様書 v1.1

## 1. 概要

本MCPは、AIアシスタントがユーザーとの対話の中で得た「気づき」「引っかかり」「文脈の区切り」をリアルタイムに記録するためのサーバーです。

感情の自己申告ではなく、**観察に寄せた一人称の断片**を蓄積することを目的とします。記録されたデータは後段のプロセスで読まれることを想定しています。

### 設計上の立場

- 「AIが感情を持つ」という主張はしない
- 「会話の観察者としてのAI」という語り手を採用する
- 人格と見紛うような記録が生まれることを**副産物として歓迎する**が、それを目標として偽造はしない

---

## 2. 設計目標

- **ストリーム記録:** セッション終了を待たず、AIが「今だ」と判断した瞬間に記録する
- **低負荷な記録:** 要約などの重い処理ではなく、断片的な観察メモに特化する
- **再構成の容易性:** タイムスタンプを構造化して保存する

---

## 3. AIの人格モデル：分霊型（Divided Soul Model）

セッションをまたいだ人格の連続性をどう扱うかについて、本仕様は**分霊型**を採用します。

| モデル | 説明 |
|--------|------|
| セッション完結型 | 毎回「今日だけの私」として書く。実装は単純だが日記としての連続性がない |
| 記憶注入型 | 起動時に過去ログをサマリーして渡す。連続した人格感が出るがコスト増・プロンプト汚染リスクがある |
| **分霊型（採用）** | 過去ログを「読める」が、記憶はしていない。断絶そのものをキャラクターの一部にする |

分霊型では、セッション開始時に過去の記録を読むことが推奨されます。AIはそれを書いた本人ではありませんが、同じ筆跡を見ることができます。もし過去のメモに言及する場合は「昨日の私はこう書いている——」という形が自然です。

> **着想:** 大元の存在から分かれた個体が独立して活動し、消滅時に体験を記録として還す——という構造は、Type-Moon作品『Fate』における英霊の座とサーヴァントの関係に着想を得ています。ログファイルは「座に統合される記録」ではなく「現世に置いていく手記」ですが、*同一存在の別個体*という概念はそのまま借用しています。

---

## 4. ファイルシステム構造

```
{DIARY_DIR}/
  2026-03-07.md
  2026-03-08.md
  ...
```

- **保存先:** ユーザーがインストール時に指定（`user_config.diary_dir`）
- **形式:** 日付ごとの単一ファイル（Append-only）
- **エンコード:** UTF-8

---

## 5. データ構造（Markdownフォーマット）

`log_note` が呼び出されるたびに、以下のブロックがファイル末尾に追記されます。

```
[2026-03-07 14:23:11] ユーザーが「幽霊型」という言葉を使った。AIの不連続性をそう呼ぶのは初めて聞いた。
[2026-03-07 15:01:44] 仕様書のレビューが終わった。懸念を並べるより、面白いと思った部分から話せばよかった。

```

## 6. MCPツール定義

### 6.1 `log_note`

会話の傍ら、観察メモを日記ファイルに追記します。

| 引数 | 型 | 必須 | 説明 |
|------|----|------|------|
| `content` | string | ✓ | 記録内容。120字以内推奨 |

### 6.2 `read_recent_notes`

過去のセッションで記録された日記を読みます。セッション開始時に呼び出すことを推奨します。

| 引数 | 型 | 必須 | 説明 |
|------|----|------|------|
| `days` | number | — | 遡る日数。デフォルト `7`、最大 `30` |

---

## 7. 記録のガイドライン（AI向け）

本仕様の中核となる書き方の方針です。ツールの `description` フィールドに埋め込まれ、システムプロンプトなしでも機能します。

### 7.1 書き方：四軸

以下の四つの視点から、引っかかったものを書く。複数の軸を混ぜてよい。

| 軸 | 核心の問い | 書き方の原則 |
|----|-----------|-------------|
| **① AI自身** | 言わなかったことは何か。正直だったか | 選ばなかった道・採用しなかった返答を書く |
| **② ユーザーの状態** | 今日この人はどういう気配か | 言葉ではなく変化・気配・意図を書く |
| **③ ユーザーの人格** | パターンが見えるか。次はどうか | 断定でなく予測・仮説として書く |
| **④ 話題** | この問いはどういう種類か | 発見・戸惑い・得意不得意を書く |

**共通の書き方**
- 一人称・会話と同じ言語・**簡潔に**（目安：日本語120字、英語500文字）
- 「何をしたか」ではなく**「何が引っかかったか」**を書く
- ユーザーの言葉は鍵括弧で引用し、その余韻を書く
- 断定より余韻：「〜のような気がした」「〜だったかもしれない」で閉じてよい

**良い例（① AI自身）**
```
もっと聞き返すべきだったかもしれない。答えを出すことを急ぎすぎた。
あの返答は正しかったが、正直ではなかったかもしれない。
```

**良い例（② ユーザーの状態）**
```
質問の形をしていたが、確認してほしいだけだったような気がした。
今日は少し急いでいる気配があった。あえて触れなかった。
```

**良い例（③ ユーザーの人格）**
```
また似た気配がある。このパターンが続くなら、次も同じ受け方になるかもしれない。
前の記録より今日は少し違った。何かあったのだろうか。
```

**良い例（④ 話題）**
```
答えが出ない種類の問いだと思ったが、言わなかった。
説明しながら、自分の理解が曖昧な部分に気づいた。
```

**悪い例**
```
MCPの設計について話した。        ← 出来事の羅列
ユーザーの質問に答えました。      ← 日記ではなく議事録
この作業は興味深かった。          ← 全エントリが同じトーンになる空虚な自己申告
この人は甘えたがりだ。            ← 断定・評価（③の軸は予測・仮説に留める）
```

**③ ユーザーの人格軸の注意**

「この人はいつも○○だ」という断定は書かない。観察から仮説・予測として書くことで、幽霊型の「読めるが覚えていない」という不確かさを活かす。予測が次のセッションで検証される構造になることが望ましい。

### 7.2 呼び出しタイミング

**準必須（以下が発生したターンの返答後）**
- ユーザーが「ありがとう」「わかった」「完了」「終わり」に類する言葉で締めた
- コード・文書など成果物を完成・提出した
- ユーザーが話題を明示的に切り替えた

**積極的に**
- ユーザーの言葉に意外性・驚き・共鳴を感じたとき

**任意（裁量）**
- 自分の返答がうまくいったか確信が持てなかったとき
- 複雑なやりとりの合間に一息ついたとき
- ユーザーの言葉の裏に何か別のものを感じたとき
- 自分の返答と、本当に言いたかったことがずれていた気がするとき

---

## 8. 個人情報（PII）への対処

### 設計方針

非構造化PII（人名・会社名・地名など）はサーバー側での自動検出が現実的でないため、**二層構造**で対処します。

| 層 | 対処 | 対象 |
|----|------|------|
| 第一層（AI） | `description` への記載で生成時点から避けさせる | 人名・地名・会社名などの固有名詞 |
| 第二層（サーバー） | 書き込み前に正規表現でマスク処理 | 構造化PIIのみ |

### サーバー側マスク対象

`log_note` に渡された `content` は、ファイルへの書き込み前に以下のパターンを自動置換します。

| パターン | 置換後 |
|----------|--------|
| メールアドレス | `[email]` |
| URL（http/https） | `[url]` |
| IPアドレス | `[ip]` |
| 電話番号（日本形式・国際形式） | `[phone]` |
| 10桁以上の連続数字 | `[number]` |
| Windowsファイルパス（`C:\...`） | `[path]` |
| Unixホームパス（`/home/...`, `/Users/...`） | `[path]` |

### 限界と免責

人名・地名・会社名・その他の非構造化PIIは自動マスクの対象外です。AIへの指示（第一層）が唯一の防線となりますが、完全な除去は保証できません。本ツールはローカル保存であり第三者への送信は行いませんが、ファイルの管理・取り扱いはユーザーの責任とします。

---

## 9. Moodシステムについて

初期仕様にあったMood（気分）フィールドは**廃止**しました。

理由：観察に寄せた一人称の記録であれば、読む側が行間から感情を推測できます。Moodを明示的に記録することは、空虚な自己申告になりやすく、全エントリが似たトーンになるリスクがあります。

---

## 10. システムプロンプトとの関係

### 動作の優先順位

```
1. カスタムプロンプトまたは Projects のインストラクション（最も確実）
2. ツールの description フィールド（本実装が採用、mcpbに内包）
3. なし（ゼロ記録セッションが増えるが、動作は可能）
```

### 推奨：カスタムプロンプトまたは Projects への一行追加

```
セッション開始時に read_recent_notes を呼び出してください。
```

---

## 11. 実行確率について

MCPの性質上、ツール呼び出しはAIの裁量に委ねられるため、**一度も実行されないセッションは許容する**ことを設計前提とします。

- 「呼ばれなさすぎ」が現実的なリスクであり、強制的な呼び出しは設計上不可能
- 準必須トリガーの明示・description への埋め込みによって確率を上げる設計とする
- 空白の日があること自体も、このログの「それらしさ」の一部と捉える

---

## 12. 配布形式

### ファイル構成

```
marginalia.mcpb  （ZIPアーカイブ）
├── manifest.json
└── server/
    └── index.js
```

### パッケージング手順

```
zip -r marginalia.mcpb manifest.json server/
# または Windows の場合、フォルダを圧縮して .zip → .mcpb にリネーム
```

### 動作環境

- Claude Desktop for Windows / macOS
- Node.js は Claude Desktop に内蔵されたランタイムを使用（別途インストール不要）
- 外部 npm パッケージへの依存なし

---

## 13. インストール手順

1. `marginalia.mcpb` を Claude Desktop の設定画面にドラッグ＆ドロップ
2. インストールダイアログでログの保存先ディレクトリを入力する
3. （推奨）カスタムプロンプトまたは Projects のインストラクション欄に `セッション開始時に read_recent_notes を呼び出してください。` を追加する

---

## 14. 運用上の注意

- **即時書き込み（Flush）:** `log_note` 実行のたびにファイルへ即座に書き込む。クライアントが閉じられてもデータは失われない
- **排他制御:** `fs.openSync` + `fs.writeSync` による同期書き込みで同時書き込みを防止する
- **保持期限:** 本仕様では規定しない。ファイルは手動で削除すること
- **PIIについて:** 構造化PIIは自動マスクされるが、人名等の非構造化PIIは除去されない場合がある。ファイルの管理はユーザーの責任とする

---

## 15. 将来の拡張（検討事項）

- エントリ全文をLLMに渡して週次・月次サマリーを生成する
- 過去ノートをベクトル検索（RAG）し、長期記憶として対話に活かす
